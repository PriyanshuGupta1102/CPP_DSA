#include <iostream>
using namespace std; 

void f(int n, char src, char dst, char hlp) {
    
    // base case

    if(n == 0) {
        return;
    }

    // Recursive Case

    // f(n) = move n disks from src('A') to dst('C') using hlp('B')

    
}

int main() {

    int n = 5;

    f(n, 'A', 'C', 'B');

    return 0;
}